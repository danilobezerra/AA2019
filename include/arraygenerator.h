#include <stdlib.h>
#include <stdio.h>
#include <time.h>

long int* allocateArray(long int);
long int* increascingOrder(long int);
long int* randomOrder(long int);
long int* decreascingOrder(long int);